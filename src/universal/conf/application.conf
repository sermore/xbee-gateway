akka {
#  loggers = ["akka.event.slf4j.Slf4jLogger"]
  loglevel = "INFO"
#  logging-filter = "akka.event.slf4j.Slf4jLoggingFilter"
  actor {
    debug {
      # DEBUG level
      #receive = on
      # autoreceive = on
      #lifecycle = on
      unhandled = on
      #event-stream = on
    }
  }
}
